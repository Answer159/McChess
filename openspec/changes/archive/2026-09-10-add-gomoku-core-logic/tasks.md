## 1. 项目骨架

- [x] 1.1 创建 `script_Gomoku/`（含 modCommon/gomokuCore 包结构与 `__init__.py`）与 `gomoku_dev/` 目录，验证 `python -c "import ..."` 在本机 py3 下可导入核心包路径

## 2. 核心逻辑类 GomokuBoard

- [x] 2.1 实现棋盘初始化与尺寸校验（宽高 5-99、支持非正方形、reset 可变尺寸），py2/3 双兼容，验证：非法尺寸构造报错、reset 后状态归零
- [x] 2.2 实现落子与回合管理（越界/占用/终局/非当轮四类失败原因、enforce_turn 开关、落子后切换执子方），验证对应单测通过
- [x] 2.3 实现增量胜负判定（4 方向、5 连及以上含长连、多线成五全部返回连线坐标）与平局判定（满盘无五连），验证横向/竖向/斜向/长连/双线成五/平局单测通过
- [x] 2.4 实现终局锁定（WON/DRAW 后 place/remove/undo 一律拒绝，仅 reset 可用），验证终局锁定单测通过
- [x] 2.5 实现删子（空位/越界/终局失败、不改变执子方、不入历史）与悔棋（历史弹栈、执子方回退、空盘失败、与删子的组合行为），验证对应单测通过
- [x] 2.6 实现 serialize/deserialize（简单类型 dict、进行中与终局状态均可无损恢复），验证恢复后局面/执子方/状态/历史一致的往返单测通过

## 3. 单测

- [x] 3.1 编写 gomoku-core 全量单测（unittest，覆盖 specs/gomoku-core 每个 scenario 至少一个用例），验证 `python gomoku_dev/test_board.py` 全绿
- [x] 3.2 在 py2/3 兼容子集上复查（无 f-string/注解/dataclass、整数除法、iteritems 等），验证本机 py3 全量通过且无 py3-only 语法

## 4. 终端调试 REPL

- [x] 4.1 实现 ASCII 棋盘渲染（行列坐标、黑白不同字符、与棋盘状态一致），验证渲染输出与手动落子一致
- [x] 4.2 实现指令循环（落子/删子/悔棋/显示/重置/退出、非法输入不崩溃、成五打印获胜方与连线、失败打印原因），验证手工冒烟：完成一局并触发胜负
